# Build artifacts
*.o
*.so
*.bc
*.dylib
*.dll
pg_fts.so

# PGXS / regression output
/results/
/output_iso/
regression.diffs
regression.out
/log/
/tmp_check/
/tmp_check_iso/
.deps/

# editor / OS
__pycache__/
*~
*.swp
.DS_Store

# other
.memelord

# agent tooling (not part of the extension).  AGENTS.md is deliberately TRACKED: it is
# the project's own rules file (measurement discipline, hygiene, release gate), written
# so the rules survive a fresh clone.  The generic per-machine steering under .agent/ and
# the tool configs stay local.
.claude/
.mcp.json
.agent/
.agent-steering-domains.md
.kiro/
.pi/


# make dist source-distribution artifact
pg_fts-*.zip

# rendered docs (built by `make html` / `nix run .#docs-html`; published to
# GitHub/Codeberg Pages by CI, never committed to main)
/doc/html/
/build/
VSIC-FiC16.pdf
bench_vac/
pg_fts-managed-service-readiness.md
result*
